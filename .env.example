# Copier ce fichier en .env.local et remplir les valeurs

# Base de données PostgreSQL
# Pour Vercel, utiliser Vercel Postgres ou Supabase
# Format: postgresql://user:password@host:port/database?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/bookmarks_manager?schema=public"

# Clé secrète JWT (générer une clé aléatoire sécurisée)
# Générer avec: openssl rand -base64 32
JWT_SECRET="votre_cle_secrete_tres_longue_et_complexe_ici"

# URL de l'application (automatique sur Vercel)
NEXTAUTH_URL="http://localhost:3000"

# API de capture d'écran (optionnel)
# Services possibles:
# - https://screenshotapi.net/
# - https://apiflash.com/
# - https://screenshot.abstractapi.com/
SCREENSHOT_API_KEY=""

# Environnement
NODE_ENV="development"